// Central types — canonical source is types.ts at root.
// This file re-exports everything for imports using @/types/index pattern.
export type { User, Salon, Client, Business, Operator, VisitLog } from '../types';